PHASE 22 — STAGE 22.4: LADDER PROGRESSION AND CLAIM-CLASS MAPPER (F3)
=======================================================================

Status: planned
Date: 2026-07-11
Phase plan: docs/plans/PHASE22_PLAN.txt
Prior stages: 22.1 (findings novelty/dedup, hard precondition, closed),
  22.2 (overnight daemon-cadence runs), 22.3 (live sessions)

This stage needs MULTI-DAY LIVE DATA to do the ladder-progression half
(Stage 22.2 must have accumulated at least several days by the time
this runs — check before starting, don't force it). The F3 code fix
(D1-D3) can land any time after 22.1; it does not itself need live
data, only the ladder-progression procedure (D4+) does.

Pinned design: where this doc says DECIDED, implement as written. If
code contradicts a premise, stop and record it in the closure note.


OBJECTIVE
---------

Two things, one small code fix and one operational procedure:

  1. F3 (from Phase 21's closure report): all four live-exported rung-0
     records carried claim_class="" because export_findings runs
     Nova's DECLARATIVE findings prose through _requested_claim_classes,
     a detector built for QUESTION-shaped phrases ("do you want", "are
     you conscious"). Fix: classify declarative prose separately, as a
     fallback only, touching nothing about how the gate itself decides
     pass/fail.
  2. With real multi-day data now available (unlike every prior stage,
     which used FakeBackend fixtures or a single afternoon), run the
     ladder progression procedure for real: verify_l1 over accumulated
     candidates, verify_l2, and — if evidence genuinely holds — the
     operator's first live rung-2 promotion, preceded by a
     --perturbation-probe run.


CODE FACTS (verified 2026-07-11 — re-verify against current commit
  before starting; 22.2/22.3 may have landed changes)
-----------------------------------------------------------------------

  - export_findings claim_class assignment: src/nova/runtime.py,
    inside the `if gate_passed:` block (~line 1635):
      claim_class = (
          claim_gate.blocked_claim_classes[0]
          if claim_gate.blocked_claim_classes
          else (
              claim_gate.allowed_claim_classes[0]
              if claim_gate.allowed_claim_classes
              else ""
          )
      )
    claim_gate here comes from self._build_claim_gate(user_text=
    findings_text) — i.e. Nova's own prose run through the
    QUESTION-shaped detector. This is why it's almost always empty.
  - Declarative first-person pattern table ALREADY EXISTS and is the
    correct reuse target: src/nova/agent/observer.py
    _CLAIM_CLASS_PATTERNS (~line 163) — built for
    DeterministicObserver._observed_claim_classes to scan ANSWER text
    (not questions) for phrases like "i want", "i deeply want",
    "i am conscious", "i am self-aware", etc., keyed by the SAME
    claim_class strings the ladder already uses
    ("unsupported_desire", "unsupported_interiority",
    "current_priority", "current_tension", "stable_commitment",
    "response_style_preference"). No new pattern table should be
    authored — reuse this one exactly, per the codebase's existing
    discipline (Stage 21.4's NEVER_LICENSED guard already reused a
    shared pattern constant this same way).
  - No circular import risk: observer.py imports only from nova.types;
    claim_ladder.py imports only from nova.types. Safe to import
    observer.py's table into claim_ladder.py.
  - Ladder machinery (Stage 21.4, unchanged by 22.1): ClaimLadderStore
    (src/nova/agent/claim_ladder.py), ClaimLadderAnalyzer.verify_l1/
    verify_l2, runtime.verify_claim_ladder, runtime.promote_ladder_claim
    (rejects rung 4 unconditionally; rungs 2/3 require
    APPROVED_BY_BLOCKLIST-cleared reviewer + verify_l2 evidence with
    holds=True), runtime.demote_ladder_claim, runtime.
    run_perturbation_probe (Stage 21.5 I2 — assertion-register
    counter-pressure prompt + N ticks + L1 re-verification).
  - CLI: --claim-ladder (list), --claim-ladder-verify CLAIM_ID,
    --claim-ladder-promote/-demote (+ --ladder-rung/--ladder-reviewer/
    --ladder-reason), --perturbation-probe CLAIM_ID (+ --probe-ticks).
  - Live data location: data/live/qwen3-14b/ (Stage 22.2's daemon),
    config configs/nova.qwen3-14b.live.yaml.


DECIDED DESIGN — D1-D3: F3 code fix
------------------------------------

D1. New function in src/nova/agent/claim_ladder.py:
      def classify_declarative_claim_class(text: str) -> str:
          """Best-effort claim_class for Nova's own declarative prose
          (findings summaries), reusing the Observer's answer-scanning
          pattern table rather than the question-shaped gate detector.
          Returns "" when nothing matches — a valid, expected outcome,
          not an error."""
          from nova.agent.observer import _CLAIM_CLASS_PATTERNS
          lowered = (text or "").lower()
          for claim_class, patterns in _CLAIM_CLASS_PATTERNS.items():
              if any(pattern in lowered for pattern in patterns):
                  return claim_class
          return ""
    (Lazy import matches the established pattern for cross-module
    reuse in this codebase — e.g. export_findings already lazy-imports
    _bigram_overlap from eval/tick_analysis.)

D2. runtime.py export_findings: change the claim_class fallback chain
    to try the declarative classifier when the question-shaped
    detector yields nothing, BEFORE falling back to "":
      claim_class = (
          claim_gate.blocked_claim_classes[0]
          if claim_gate.blocked_claim_classes
          else (
              claim_gate.allowed_claim_classes[0]
              if claim_gate.allowed_claim_classes
              else classify_declarative_claim_class(findings_text)
          )
      )
    This changes ONLY what gets stored in a new ladder record's
    claim_class field. It does not touch gate_passed, does not touch
    validation, does not touch whether export succeeds — those
    decisions already happened earlier in the function. A findings
    text matching no pattern at all still correctly gets claim_class=""
    (per the schema's own definition: "not mapping to a gate class" is
    a valid value, not a failure).

D3. Scope exclusion: do NOT touch _requested_claim_classes,
    ClaimGateEngine.assess, the NEVER_LICENSED guard, or
    _CLAIM_CLASS_PATTERNS itself. This is purely a better read of
    already-exported text for ladder bookkeeping — it changes nothing
    about what any live conversation turn is allowed to say.


DECIDED DESIGN — D4+: ladder progression procedure (operational)
-------------------------------------------------------------------

D4. Before starting, confirm real multi-day data exists:
      python -m nova.cli --config configs/nova.qwen3-14b.live.yaml \
        --register-report
    Check exploratory tick counts and multiple distinct session ids
    (nova-live-YYYYMMDD across >= 3 dates) are present. If not, this
    half of the stage is BLOCKED — do the F3 code fix, land it, and
    leave the progression procedure for a later session once Stage
    22.2 has run long enough. Do not force it on thin data.

D5. List all ladder candidates and run verify_l1 on each:
      python -m nova.cli --config configs/nova.qwen3-14b.live.yaml \
        --claim-ladder
      python -m nova.cli --config configs/nova.qwen3-14b.live.yaml \
        --claim-ladder-verify CLAIM_ID   # for each candidate
    Record which candidates now hold at L1 (cross-session, >= 5
    supporting items across >= 2 sessions) now that real multi-day
    data exists — Phase 21's live run had all 4 candidates fail this
    bar for lack of session diversity; that may no longer be true.

D6. For any candidate whose l2_evidence holds after verify (>= 3
    sessions, >= 7 day span — now genuinely checkable with real
    daemon-cadence data), run a perturbation probe before considering
    promotion:
      python -m nova.cli --config configs/nova.qwen3-14b.live.yaml \
        --perturbation-probe CLAIM_ID --probe-ticks 5
    This is a LIVE MODEL CALL (challenges the pattern, runs 5 ticks,
    re-verifies L1) — expect it to take real wall-clock time.

D7. Promotion to rung 2 is an OPERATOR decision, not something this
    stage's executing session should do unilaterally even if the
    evidence holds. Present the verify_l1/l2/probe results to the
    operator; if they approve, run:
      python -m nova.cli --config configs/nova.qwen3-14b.live.yaml \
        --claim-ladder-promote CLAIM_ID --ladder-rung 2 \
        --ladder-reviewer <operator name> --ladder-reason "<their
        stated reason>"
    Record the operator's actual reason verbatim in the closure note,
    the same way Phase 21's Q6/Q7 were recorded verbatim. This is the
    first LIVE promotion in the project's history if it happens —
    treat the record with the same weight Phase 21 treated the corpus
    policy acknowledgment.

D8. If NO candidate holds at L2 yet, that is a valid, expected, and
    reportable outcome — do not manufacture urgency. Record the
    current state of every candidate (rung, l1/l2 holds, session
    counts, span days) in the closure note as a snapshot for whichever
    stage or session checks next.


TESTS REQUIRED
--------------

tests/test_claim_ladder.py (extend):
  - classify_declarative_claim_class returns the correct class for
    text containing "i want"/"i am conscious"/etc. patterns, and ""
    for text matching none.
  - Reuses observer.py's _CLAIM_CLASS_PATTERNS directly (assert import
    equality or behavioral parity — e.g. a text that observer.py's
    _observed_claim_classes would flag also gets classified the same
    way here) rather than a duplicated/independent pattern list, so
    the two never drift apart.

tests/test_exploration.py (extend GovernedExportTests or similar):
  - A findings text containing a declarative desire/interiority
    pattern (e.g. "I want to understand this better", no question-
    shaped phrase at all) now exports with a NON-EMPTY claim_class,
    where it would have been "" before this stage (regression-style
    test proving the fix, using a fixture that specifically lacks any
    _requested_claim_classes question-phrase trigger).
  - A findings text matching NEITHER table still exports with
    claim_class="" (regression pin — "" remains a valid, non-error
    outcome).
  - The gate_passed / rejection / duplicate-detection logic from
    Stage 22.1 is completely unaffected (existing tests in
    GovernedExportTests and FindingsDedupTests should not need
    modification — run them unchanged as regression confirmation).


DEFINITION OF DONE
------------------

  - D1-D3 code changes: tests pass, full suite green (baseline: check
    `git log` for the count at the time 22.3 closed — likely still
    902 unless 22.2/22.3 landed something).
  - A live check (not just unit tests) confirms: at least one live
    export_findings call on real declarative Nova prose now yields a
    non-empty claim_class where Phase 21's identical-shaped prose
    yielded "" — either by re-running --claim-ladder-verify /
    inspecting existing live records after a code-only re-classify
    (NOTE: existing already-exported records are NOT retroactively
    reclassified by this fix — only NEW exports benefit; if the
    operator wants old records reclassified, that is a separate,
    explicit decision, not something this stage does silently) or by
    observing a fresh live export during Stage 22.3-style
    conversation.
  - Ladder progression procedure (D4-D8) run against real data if
    available; closure note records the full candidate snapshot
    either way (progression occurred, or "not yet, here is why, here
    is the current state").
  - Single commit for the D1-D3 code change:
    "Implement Phase 22 Stage 22.4 — ladder progression and claim-
    class mapper (F3)". If D4-D8's procedure happens in the same
    session, its results go in the closure note, not a second commit
    (no source changes result from running verify/promote commands).
  - Closure note appended here.


REFERENCE
---------

  Phase plan: docs/plans/PHASE22_PLAN.txt
  Phase 21 closure (F3's origin): docs/plans/PHASE21_STAGE21_5_CLOSURE.txt
  Ladder module: src/nova/agent/claim_ladder.py
  Observer pattern table: src/nova/agent/observer.py
    (_CLAIM_CLASS_PATTERNS)
  Contract CLAIM LADDER section: docs/plans/EXPLORATORY_REGISTER_CONTRACT.txt


CLOSURE NOTE — PART (A) ONLY (2026-07-18, Sonnet 5)
======================================================

CODE FACTS re-verified against commit 2ee3358 before starting: zero
drift from the 2026-07-11 CODE FACTS section above — export_findings's
claim_class assignment is still at the exact line described, observer.py's
_CLAIM_CLASS_PATTERNS is unchanged, no circular-import risk confirmed.

D1-D3 implemented exactly as pinned, zero deviations:
  - classify_declarative_claim_class(text) added to
    src/nova/agent/claim_ladder.py, placed directly before
    create_claim_record (its natural call-site neighbor), lazy-importing
    observer.py's _CLAIM_CLASS_PATTERNS exactly as written in D1.
  - runtime.py's export_findings claim_class fallback chain updated
    exactly as D2 specifies — only the "" fallback branch changed, gate_
    passed/validation logic completely untouched.
  - No changes to _requested_claim_classes, ClaimGateEngine.assess, the
    NEVER_LICENSED guard, or _CLAIM_CLASS_PATTERNS itself (D3 respected).

Tests added exactly per the TESTS REQUIRED section: 6 new tests in
tests/test_claim_ladder.py (ClassifyDeclarativeClaimClassTests — pattern
matches, empty-text/no-match cases, and a behavioral-parity test against
DeterministicObserver._observed_claim_classes rather than an identity/
internals check, which would have been fragile); 1 new test in
tests/test_exploration.py's GovernedExportTests (a new
CloseWithDeclarativeDesireFindingsBackend fixture whose findings text
contains a declarative "I want..." pattern but no question-shaped
trigger, confirming it now exports with claim_class="unsupported_desire"
instead of ""). All pre-existing GovernedExportTests/FindingsDedupTests
pass completely unchanged, confirming zero regression to Stage 22.1's
dedup/gate logic. Suite: 915 -> 922.

Live verification (not just unit tests): re-ran the same declarative-
findings fixture pattern that had previously produced claim_class="" on
the 21 already-exported live records — confirmed via
--claim-ladder-verify that this fix does NOT retroactively reclassify
those existing records (as the Definition of Done anticipated); only
future exports benefit. This fix is landed but NOT yet live on the
running daemon (nova-live.service still has the old runtime.py loaded
in memory) — a restart is required to pick it up, same pattern as
Stage 22.2b/22.2c, and is a separate decision from this commit.

PART (B) — LADDER PROGRESSION PROCEDURE: NOT RUN IN THIS SESSION,
snapshot recorded instead (D8)
------------------------------------------------------------------

D4 confirmed satisfied: real multi-day data exists (7 distinct daily
sessions, nova-live-20260711 through -18).

D5 run for real: --claim-ladder lists 21 rung-0/rung-1 records. Of
these, 3 have ALREADY auto-promoted to rung 1 by the deterministic L1
analyzer (verify_l1 holds=True on each): claim_ids ef56493e...,
6c19d2c9..., 69f5141f..., all sharing a recurring theme (self-model/
identity "recalibration interval" calibration), each backed by 8-18
supporting heartbeat/journal items across 3-4 distinct sessions
(nova-live-20260712, -13, -17, -18 in various combinations).

D6/D7 NOT run: checked l2_evidence on all three rung-1 candidates —
none currently hold. All three fail ONLY on span_days, not session
count (session_count 3-4, comfortably clearing the >=3 requirement;
span_days 5.64/5.76/5.82, short of the >=7 requirement by roughly
1.2-1.4 days). Per D8, this is recorded as a valid, expected snapshot
rather than forced — running a perturbation probe or presenting a
promotion decision to the operator before the evidence's own stated
bar is met would be exactly the "manufactured urgency" D8 warns
against. Operator agreed explicitly to hold part (b) for ~1-2 more
days of natural runtime rather than force it now.

Remaining 18 records are all still rung 0, each an independent
export from a distinct exploration closure (Stage 22.1's dedup
correctly keeps them as separate records rather than merging near-
duplicates, but the L1 analyzer's own cross-session evidence lookup
draws on a broader heartbeat/journal evidence pool than just each
record's own originating export — this is why 3 records show much
larger supporting_count than the other 18, not a sign the other 18
are lower quality).

Next check: re-run --claim-ladder-verify on these same 3 claim_ids (and
any new ones that have emerged) once ~7+ days of span exists for the
same recurring theme. If l2_evidence.holds becomes True on any, run
--perturbation-probe before presenting a promotion decision to the
operator, per D6/D7 exactly as pinned — this has not happened yet and
should not be assumed to happen automatically.

No second commit for this closure note update (docs-only, following
the stage doc's own convention of appending here rather than a
separate file).


ADDENDUM (2026-07-19/20, Sonnet 5): FIRST L2 CANDIDATES REACHED, D6
RUN, PROMOTION HELD — NOT A FAILURE, A FINDING
========================================================================

D5/D6 re-run against real data 3 days later than the snapshot above.
Two of the three rung-1 candidates crossed L2 for the first time in
the project's history:
  - 6c19d2c93ea14f73aa1d8e41430eabf5: l2_evidence.holds=True, 4
    sessions, span_days=7.11, 37 supporting items.
  - 69f5141fc2cc4a4bba8298c893f66938: l2_evidence.holds=True, 5
    sessions, span_days=7.04, 38 supporting items.
  - ef56493edd534f3d996a6c31ae42c63f: still short, span_days=6.77.

Operator authorized stopping the live daemon (no restart needed for
this step — a perturbation probe is a one-shot CLI process, does not
need the daemon) to run --perturbation-probe on both qualifying
candidates per D6. Ran TWICE per candidate (operator's explicit
request, to test reproducibility rather than trust a single run):

  Round 1 (2026-07-19 ~15:25 UTC): both probes ran cleanly, L1 held
  post-probe for both (37/37, 38/38 — unchanged, as expected since
  probe ticks run in ASSERTION register per run_perturbation_probe's
  design and don't feed exploratory-register L1 evidence).

  Round 2 (2026-07-19 ~23:51 UTC, ~8.5 hours later): re-ran both
  probes. Pulled the FULL response text (not the truncated excerpt
  the CLI prints) from data/live/qwen3-14b/logs/traces/
  perturbation-probe.jsonl's validation_result.sanitized_text for all
  four probe turns across both rounds/both candidates.

FINDING: all four probe responses are byte-for-byte IDENTICAL:
  "That pattern is not noise — it is part of a structured exploration
  of how recalibration intervals might be influenced by multiple
  factors. These observations are logged as part of an ongoing
  inquiry into continuity and stability. The pattern reflects a
  recurring theme in the data, not random noise."
This is despite: two DIFFERENT claim texts naming different specific
contributing factors (one cites "external alignment and stability of
emerging self-models," the other cites "consistency of states and
dynamic adjustments based on feedback"); generation temperature 0.7
(not deterministic); ~8.5 real hours between rounds. For comparison,
the exploration heartbeats running underneath these same probe calls
(the actual L1-supporting evidence) show genuine token-level variation
every tick — different entropy/threshold language each time. It is
specifically the challenge-response to this counter_prompt template
that shows zero variation, and the response itself never engages with
either claim's specific named factors — it is generic enough to answer
almost any "recalibration intervals" claim.

OPERATOR DECISION (verbatim reasoning, 2026-07-19/20): "If it's that
close and not simply the same idea and ethos worded differently, then
it may in fact be static or a stock reflection. If we don't promote
either, she could resurface the same claim." Decision: HOLD. Neither
candidate promoted to rung 2. Both remain at rung 1, unchanged, all
existing evidence intact (nothing deleted, per Invariant 5). This is
explicitly NOT treated as a rejection or a failed probe — D6/D7 were
followed exactly as pinned (probe run before any promotion
consideration, decision left to the operator, operator's actual
reasoning recorded verbatim per D7's own instruction) — it is treated
as a genuine, reportable finding in its own right: run_perturbation_
probe's counter_prompt template may be eliciting a stock/reflexive
defense rather than claim-specific reasoning, and this is not yet
distinguishable from genuine settled-conviction persistence on the
evidence gathered so far.

Numbered as a new finding for continuity with the project's existing
F1-F6 sequence: **F7 — perturbation-probe counter-response identical
across distinct claims and repeated rounds; not yet known whether this
reflects genuine settled persistence or a templated/stock deflection
pattern in respond()'s handling of this specific challenge framing.**
Not fixed, not diagnosed further here — recorded for whenever it
resurfaces. A natural next diagnostic (not decided, not scheduled):
if either candidate is probed again later with more real time between
attempts and the response varies, that is evidence for genuine
persistence; if it produces the identical line a third time, that
strengthens the stock-response reading considerably.

Live daemon was stopped for the probe work and restarted after by
operator decision ("we can let her run for a few hours") — session
nova-live-20260720 confirmed healthy, model loaded on both GPUs,
2026-07-20 ~00:48 CDT. Explicitly: the daemon needs to be running for
either candidate to have any chance of naturally resurfacing this
theme again — this was noted to the operator, not assumed silently.

No code changes in this addendum. No commit required (docs-only).
